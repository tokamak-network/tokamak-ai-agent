# 체크포인트 시스템 테스트 가이드

## 개요

체크포인트 시스템은 에이전트가 작업을 수행하는 동안 각 단계마다 워크스페이스의 스냅샷을 저장하고, 필요시 이전 상태로 롤백할 수 있는 기능입니다.

## 테스트 시나리오

### 1. 기본 체크포인트 생성 테스트

**목적**: 에이전트가 작업 단계마다 체크포인트를 자동으로 생성하는지 확인

**절차**:
1. VS Code에서 프로젝트 폴더 열기
2. `Cmd+Shift+I` (Mac) / `Ctrl+Shift+I` (Windows)로 Tokamak Chat 열기
3. 모드를 **Agent** 모드로 변경
4. 다음 요청 입력:
   ```
   Create a simple React component called TestComponent with a button that shows an alert
   ```
5. 에이전트가 계획을 수립하고 실행하기 시작하면 Plan Panel 아래에 **Checkpoints** 패널이 나타나는지 확인
6. 각 단계가 실행될 때마다 새로운 체크포인트가 생성되는지 확인

**예상 결과**:
- Plan Panel 아래에 Checkpoints 패널이 표시됨
- 각 단계 실행 전에 체크포인트가 생성됨
- 체크포인트 목록에 단계 설명, 타임스탬프, 파일 개수가 표시됨

---

### 2. 체크포인트 비교 테스트

**목적**: 체크포인트와 현재 워크스페이스의 차이를 확인할 수 있는지 테스트

**절차**:
1. 위 테스트에서 체크포인트가 생성된 상태에서 진행
2. 에이전트가 파일을 생성/수정한 후
3. Checkpoints 패널에서 **Compare** 버튼 클릭
4. VS Code의 Diff 뷰가 열리는지 확인
5. 변경된 내용이 올바르게 표시되는지 확인

**예상 결과**:
- Diff 뷰가 열림
- 왼쪽: 체크포인트 시점의 파일 내용
- 오른쪽: 현재 파일 내용
- 변경사항이 하이라이트되어 표시됨

---

### 3. 체크포인트 복원 테스트 (Workspace Only)

**목적**: 워크스페이스 파일만 복원하는 기능 테스트

**절차**:
1. 에이전트가 여러 파일을 생성/수정한 상태에서
2. 수동으로 일부 파일을 추가로 수정하거나 삭제
3. Checkpoints 패널에서 이전 체크포인트의 **Restore** 버튼 클릭
4. 확인 대화상자에서 "Yes" 선택
5. 파일들이 체크포인트 시점의 상태로 복원되는지 확인

**예상 결과**:
- 확인 대화상자가 표시됨
- "Yes" 선택 시 파일들이 복원됨
- 수동으로 변경한 내용이 사라지고 체크포인트 시점의 상태로 돌아감
- Plan 상태는 유지됨 (워크스페이스만 복원)

---

### 4. 체크포인트 복원 테스트 (Full Restore)

**목적**: 워크스페이스와 작업 상태를 모두 복원하는 기능 테스트

**절차**:
1. 에이전트가 여러 단계를 완료한 상태에서
2. Checkpoints 패널에서 중간 단계의 체크포인트를 찾아 **Restore** 버튼 클릭
3. 확인 대화상자에서 "Yes" 선택
4. 워크스페이스와 Plan 상태가 모두 복원되는지 확인

**예상 결과**:
- 파일들이 복원됨
- Plan Panel에 체크포인트 시점의 Plan 상태가 표시됨
- 에이전트가 해당 시점부터 다시 작업을 계속할 수 있음

---

### 5. 체크포인트 삭제 테스트

**목적**: 불필요한 체크포인트를 삭제할 수 있는지 테스트

**절차**:
1. 여러 체크포인트가 생성된 상태에서
2. Checkpoints 패널에서 특정 체크포인트의 **×** 버튼 클릭
3. 확인 대화상자에서 "Yes" 선택
4. 체크포인트가 목록에서 제거되는지 확인

**예상 결과**:
- 확인 대화상자가 표시됨
- "Yes" 선택 시 체크포인트가 삭제됨
- 목록에서 해당 체크포인트가 사라짐

---

### 6. 체크포인트 새로고침 테스트

**목적**: 체크포인트 목록을 수동으로 새로고침할 수 있는지 테스트

**절차**:
1. Checkpoints 패널이 표시된 상태에서
2. 패널 헤더의 **🔄** 버튼 클릭
3. 체크포인트 목록이 업데이트되는지 확인

**예상 결과**:
- 체크포인트 목록이 최신 상태로 업데이트됨
- 새로 생성된 체크포인트가 표시됨

---

### 7. 에러 발생 시 체크포인트 활용 테스트

**목적**: 에러 발생 후 체크포인트를 사용하여 안전하게 롤백하는지 테스트

**절차**:
1. 에이전트가 작업을 수행 중인 상태에서
2. 수동으로 파일을 삭제하거나 손상시켜 에러 발생시키기
3. 에이전트가 에러를 감지하고 Fixing 상태로 전환되는지 확인
4. Fixing이 실패하면 Checkpoints 패널에서 이전 체크포인트로 복원
5. 복원 후 에이전트가 정상적으로 작업을 계속할 수 있는지 확인

**예상 결과**:
- 에러 발생 시 체크포인트가 생성됨
- 이전 체크포인트로 복원 가능
- 복원 후 정상 상태로 돌아감

---

### 8. 대규모 프로젝트에서의 성능 테스트

**목적**: 많은 파일이 있는 프로젝트에서도 체크포인트가 정상 작동하는지 테스트

**절차**:
1. 100개 이상의 파일이 있는 프로젝트에서 테스트
2. 에이전트 모드로 작업 시작
3. 체크포인트 생성 시간이 합리적인지 확인 (10초 이내)
4. 체크포인트 복원이 정상적으로 작동하는지 확인

**예상 결과**:
- 체크포인트 생성이 완료됨 (타임아웃 없이)
- 복원 시 모든 파일이 올바르게 복원됨
- 성능 저하가 크지 않음

---

## 체크리스트

테스트 완료 후 다음 항목들을 확인하세요:

- [ ] 체크포인트가 각 단계마다 자동으로 생성됨
- [ ] Checkpoints 패널이 Agent 모드에서만 표시됨
- [ ] 체크포인트 목록에 올바른 정보가 표시됨 (설명, 날짜, 파일 개수)
- [ ] Compare 기능이 정상 작동함
- [ ] Restore 기능이 정상 작동함 (워크스페이스만)
- [ ] Restore 기능이 정상 작동함 (전체 복원)
- [ ] Delete 기능이 정상 작동함
- [ ] Refresh 기능이 정상 작동함
- [ ] 에러 발생 시에도 체크포인트가 생성됨
- [ ] 대규모 프로젝트에서도 성능 문제 없음

---

## 알려진 제한사항

1. **파일 크기 제한**: 10MB 이상의 파일은 체크포인트에 포함되지 않습니다.
2. **파일 개수 제한**: 최대 1000개 파일까지만 체크포인트에 포함됩니다.
3. **제외된 디렉토리**: `node_modules`, `.git`, `dist`, `build` 등은 체크포인트에서 제외됩니다.
4. **비동기 로딩**: 체크포인트 로드는 비동기로 수행되므로 초기 로딩 시 약간의 지연이 있을 수 있습니다.

---

## 문제 해결

### 체크포인트가 생성되지 않는 경우
- ExtensionContext가 제대로 설정되었는지 확인
- 워크스페이스 폴더가 열려있는지 확인
- 콘솔에서 에러 메시지 확인

### 체크포인트 복원이 실패하는 경우
- 파일 권한 확인
- 디스크 공간 확인
- VS Code의 WorkspaceEdit API 제한 확인

### 체크포인트 목록이 표시되지 않는 경우
- Agent 모드인지 확인
- `getCheckpoints` 메시지가 전송되었는지 확인
- 웹뷰 콘솔에서 JavaScript 에러 확인
